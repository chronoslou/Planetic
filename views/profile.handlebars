
<div class="box">
  <div class="field">
        <label class="label">Username: {{name}}</label>
        <label class="label">Email: {{email}}</label>
        <label class="label">Home City: {{location_id}}</label>
        </div>
        <div class="field">
        <label class="label">Pro Tip:</label>
        <div class="control">
            <textarea class="textarea" placeholder={{proTip}}></textarea>
        </div>
        </div>
        <div class="field is-grouped">
        <div class="control">
            <button class="button is-link">Change Pro Tip</button>
        </div>
        <div class="control">
            <button class="button is-link is-light">Cancel</button>
        </div>
    </div>
</div>